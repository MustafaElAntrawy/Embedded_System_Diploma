#include <stdio.h>

int main(){
	float a;
	printf("Enter a numbers: ");
	fflush(stdin); fflush(stdout);
	scanf("%f", &a);

	if(a == 0) printf ("You entered zero.");
	else if (a > 0) printf("%f is positive", a);
	else printf("%f is negative", a);

	return 0;
}
