#include <stdio.h>

int main (){
	char text[100];
	char reversed[100];
	printf("Enter a String: ");
	fflush(stdin); fflush(stdout);
	gets(text);
	int l = strlen(text), i;
	for (i = l; i > 0; --i);
	printf("Length of string: %d", i);


	return 0;
}
